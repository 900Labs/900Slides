name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-macos:
    name: Build macOS .app
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.92.0
        uses: dtolnay/rust-toolchain@1.92.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install npm dependencies
        run: npm ci --prefix apps/desktop

      - name: Build the macOS .app (ad-hoc signed)
        run: npm run tauri:build --prefix apps/desktop -- --bundles app
        env:
          # Ad-hoc signing (no Apple Developer account needed). The "-"
          # identity signs locally without notarization. Users right-click
          # → Open to bypass Gatekeeper on first launch.
          APPLE_SIGNING_IDENTITY: "-"

      - name: Upload .app artifact
        uses: actions/upload-artifact@v4
        with:
          name: 900Slides-macos
          path: apps/desktop/src-tauri/target/release/bundle/macos/*.app
          if-no-files-found: error

  build-linux:
    name: Build Linux .deb and .AppImage
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.92.0
        uses: dtolnay/rust-toolchain@1.92.0

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install npm dependencies
        run: npm ci --prefix apps/desktop

      - name: Build Linux bundles
        run: npm run tauri:build --prefix apps/desktop -- --bundles deb,appimage

      - name: Upload .deb artifact
        uses: actions/upload-artifact@v4
        with:
          name: 900Slides-linux-deb
          path: apps/desktop/src-tauri/target/release/bundle/deb/*.deb
          if-no-files-found: error

      - name: Upload .AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: 900Slides-linux-appimage
          path: apps/desktop/src-tauri/target/release/bundle/appimage/*.AppImage
          if-no-files-found: error

  build-windows:
    name: Build Windows offline NSIS installer
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.92.0
        uses: dtolnay/rust-toolchain@1.92.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install npm dependencies
        run: npm ci --prefix apps/desktop

      - name: Build installer including WebView2 runtime
        run: npm run tauri:build --prefix apps/desktop -- --bundles nsis

      - name: Upload offline installer
        uses: actions/upload-artifact@v4
        with:
          name: 900Slides-windows-offline
          path: apps/desktop/src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
